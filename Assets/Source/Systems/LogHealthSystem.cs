using Entitas;
using UnityEngine;

/*public sealed class LogHealthSystem : IExecuteSystem
{
    readonly IGroup<GameEntity> _entitas;

    public LogHealthSystem(Contexts context)
    {
        _entitas = context.game.GetGroup(GameMatcher.Health);
    }
    public void Execute()
    {
        foreach (var entity in _entitas)
        {
            float health = entity.health.health;
            Debug.Log(health);
        }
    }
}
*/
